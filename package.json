{
  "name": "@bazaar-buidlers/subgraph",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph codegen && graph build",
    "create": "graph create --node http://localhost:8020/ bazaar-buidlers/localhost",
    "remove": "graph remove --node http://localhost:8020/ bazaar-buidlers/localhost",
    "deploy": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 --network localhost bazaar-buidlers/localhost"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.37.2",
    "@graphprotocol/graph-ts": "0.29.1"
  }
}
